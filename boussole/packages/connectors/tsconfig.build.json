{
  "extends": "./tsconfig.json",
  // `src/testing` est compilé : il est exposé comme sous-chemin
  // `@boussole/connectors/testing` pour les tests des autres paquets.
  "exclude": ["src/**/*.test.ts", "dist"]
}
