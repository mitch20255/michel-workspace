/**
 * Jeux de données partagés par les tests des autres paquets.
 *
 * Exposé comme sous-chemin `@boussole/core/testing` plutôt que depuis le point
 * d'entrée principal : ces fixtures ne doivent jamais entrer dans un bundle
 * applicatif.
 */
export * from './fixtures.js';
