{
  "name": "@boussole/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API HTTP de Boussole : profil, ingestion, scoring, CRM, documents.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@boussole/connectors": "workspace:*",
    "@boussole/core": "workspace:*",
    "@boussole/db": "workspace:*",
    "@boussole/documents": "workspace:*",
    "@boussole/llm": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.1",
    "zod": "^4.4.3"
  }
}
